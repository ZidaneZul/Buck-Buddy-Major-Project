using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectiveListSorter : MonoBehaviour
{
    public ObjectiveDataHolder[] objDataList;

    public GameObject dynamicShoppingListContent;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
